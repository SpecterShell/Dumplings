name: Test
on:
  workflow_dispatch:
jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: .\Index.ps1 -Test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
