interface:
  display_name: "Author WinGet Manifest"
  short_description: "Craft and update WinGet manifests safely"
  default_prompt: "Use $author-winget-manifest to create a working WinGet manifest early and refine it as installer, locale, release, and VM evidence is collected."
