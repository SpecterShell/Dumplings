interface:
  display_name: "Analyze WinGet Installer"
  short_description: "Identify installer types and ARP behavior"
  default_prompt: "Use $analyze-winget-installer to inspect a Windows installer and determine WinGet manifest fields safely."
