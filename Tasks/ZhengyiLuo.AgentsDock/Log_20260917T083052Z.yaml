Version: 1.0.2
Installer:
- Architecture: x64
  InstallerUrl: https://github.com/ZhengyiLuo/AgentsDock-Releases/releases/download/v1.0.2/AgentsDock-1.0.2-win-x64.exe
Locale:
- Locale: en-US
  Key: ReleaseNotes
  Value: |-
    AgentsDock 1.0.2
    Stable desktop update from 1.0.1. Team Network remains a beta feature.
    Change a saved Team Network host address
    - Use Change host address in Team Network or a saved approved connection,
      including when the host is offline. Enter the host's new IP and port in one
      small dialog; no new invitation or approval is needed.
    - Verify the new endpoint against the connection's existing trusted host and
      team before saving. Preserve membership, credentials and chat routes.
      An incorrect address or a different host cannot silently replace that trust.
    - Keep inactive saved connections inactive. Recheck the current connection
      after a successful change, with no new background polling or automatic
      address discovery.
    This action requires AgentsServer 1.0.2 on the member's own server. Updating
    only the team host does not change members' saved addresses. Older servers show
    an update explanation instead of attempting an unsafe rejoin.
    Independent Side chat
    - Ask a side question from the chat header and continue it in the inspector's
      Side chat tab. The Details tab remains alongside it.
    - Use recent visible conversation context with Codex or Claude, independently
      of the main agent. Sending or cancelling a side question does not steer,
      interrupt, queue a main turn, pause its goal or enter its history.
    - Keep the side conversation and draft while switching tabs or chats. Side
      history is temporary; attachments, hidden reasoning and tool results are not
      included. Requires a supporting server and provider.
    Search and clarity
    - Keep immediate clicks from being discarded during an identity reset, both
      when opening the host-address editor and loading a Mail/Bulletin attachment
      preview.
    - Search Mail and Bulletin with explicit, indexed server queries and paged
      results. Typing does not trigger repeated network searches.
    - Keep changed-file summaries visible outside collapsed progress.
    - Show a quiet Syncing… status when an active chat is reconnecting instead
      of a misleading warning about an active turn.
    Downloads and updates
    - Stable-channel automatic updates are published to the public AgentsDock
      repository and the legacy desktop update feed.
    - macOS builds are signed and notarized. Windows installers remain unsigned;
      Windows may display its usual publisher warning. Linux packages are included.
    - The matching standalone AgentsServer 1.0.2 release contains the new
      endpoint recovery control and the preceding beta collaboration improvements.
      Publishing either release does not force a running server to restart.
- Locale: en-US
  Key: ReleaseNotesUrl
  Value: https://github.com/ZhengyiLuo/AgentsDock-Releases/releases/tag/v1.0.2
ReleaseTime: 2026-09-17T08:02:24.0000000Z
