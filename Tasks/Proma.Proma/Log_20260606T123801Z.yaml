Version: 0.11.1
Installer:
- Architecture: x64
  InstallerUrl: https://cdn.proma.cool/releases/win-x64/Proma-0.11.1-setup.exe
Locale:
- Locale: zh-CN
  Key: ReleaseNotesUrl
  Value: https://github.com/proma-ai/Proma/releases
- Locale: zh-CN
  Key: ReleaseNotes
  Value: |-
    Proma v0.11.1
    对比基准：v0.11.0
    这一版继续打磨 v0.11.0 重做后的 Agent 侧边栏：会话列表的视觉权重更克制、展开节奏更可控，同时修了一个三点菜单浮层会漂到屏幕左上角的恼人问题。
    新功能
    - 侧边栏会话列表视觉权重与展开节奏优化 — 项目下会话过多时的「展开显示 N 个更多会话」按钮改为「显示更多」，每次只增量展开 10 条，避免一次性把上百条会话全部铺开；按钮文字去掉 font-medium 加粗，回归普通文本权重。项目内会话行间距从 gap-px 调整为 gap-0.5，与其它列表保持一致。状态色块也重新分了主次：running（蓝）和 completed（绿）取消常驻填充背景，仅保留左侧色条作为状态标识；blocked（橙）作为真正需要立即关注的状态，保留填充背景。选中的会话与当前项目也不再常驻灰底，hover 灰底只在鼠标真正划过时才出现，消除「整条一直像在被 hover」的视觉错觉
    Bug 修复
    - 会话三点菜单浮层不再漂到屏幕左上角 — SessionItemActions 此前用 hidden / group-hover:flex 控制按钮组显隐，菜单打开后鼠标一旦移开会话条目，父级 group:hover 就失效，按钮组被切回 display:none，Radix Popper 拿不到 trigger 矩形，浮层就漂到视口 (0,0)。现在菜单 open 时强制保持按钮组挂载与可见，关闭后再恢复 hover 切换，菜单始终贴在触发按钮旁边
- Locale: zh-CN
  Key: ReleaseNotesUrl
  Value: https://github.com/proma-ai/Proma/releases/tag/v0.11.1
ReleaseTime: 2026-06-06T11:48:28.5330000Z
