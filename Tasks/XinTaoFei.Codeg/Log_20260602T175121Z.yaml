Version: 0.14.11
Installer:
- Architecture: x64
  InstallerType: nullsoft
  InstallerUrl: https://github.com/xintaofei/codeg/releases/download/v0.14.11/codeg_0.14.11_x64-setup.exe
- Architecture: arm64
  InstallerType: nullsoft
  InstallerUrl: https://github.com/xintaofei/codeg/releases/download/v0.14.11/codeg_0.14.11_arm64-setup.exe
Locale:
- Locale: en-US
  Key: ReleaseNotes
  Value: |-
    - feat(delegation): Checking a delegation's status with the wait time set to 0 now waits until the task finishes, so you get the result without polling.
    - perf(message): Streaming or switching one session no longer re-renders the others — multi-session layouts stay smooth.
    - fix(message): The streaming sub-agent card's "Sub-agent starting…" placeholder is now localized across all 10 languages.
    - fix(message): Ordered markdown lists now render their markers, and markers stay on the same line as the item text.
    - fix(message-input): Clipboard images can now be pasted into the input on Linux.
    - fix(goal): Codex goal updates now render as active cards.
    - fix(delegation): Reloaded conversations now show the correct delegation status and grouping.
- Locale: zh-CN
  Key: ReleaseNotes
  Value: |-
    - 功能(委派)：获取委派状态时，将等待时间设为 0 会一直等到任务结束，无需轮询即可拿到结果。
    - 性能(消息)：流式输出或切换某个会话时不再重新渲染其他会话，多会话布局更流畅。
    - 修复(消息)：流式子代理卡片的占位标题“子代理启动中…”已在全部 10 种语言中本地化。
    - 修复(消息)：有序 Markdown 列表现在能正确显示序号，且列表标记与条目文本保持在同一行。
    - 修复(消息输入)：现在可以在 Linux 上将剪贴板图片粘贴到输入框。
    - 修复(目标)：Codex 的目标更新现在会渲染为进行中的卡片。
    - 修复(委派)：重新加载的会话现在会显示正确的委派状态与分组。
- Locale: en-US
  Key: ReleaseNotesUrl
  Value: https://github.com/xintaofei/codeg/releases/tag/v0.14.11
ReleaseTime: 2026-06-02T15:41:10.0000000Z
