Version: 2.3.4
Installer:
- InstallerUrl: https://www.everauto.net/down/zTasker_2.3.4_Setup.exe
Locale:
- Locale: zh-CN
  Key: ReleaseNotes
  Value: |-
    新增：
    **键盘热键，新增继续转发选项，可以不再唯一独占该热键
    **提醒窗口增加在哪个屏幕上显示的设置，适用于多显示器的电脑
    **自动触发，Cron 表达式，增加多行功能，更加灵活强大，任意指定多个时间点
    **任务->网络分类，增加 TCP/UDP/WebSocket 发送功能
    任务->输入分类，录制键鼠动作，播放速度增加更快的选项，谨慎使用
    任务->程序分类，增加获取进程 ID、目录、命令行、各种占用
    变量插入，增加任务信息：序号、id、名称、参数，可以用做提示或者转发到某处
    复合任务，变量判断，被判断内容增加变量插入，现在可以支持判断两个变量了

    改进：
    **当主动清空任务后，下次启动时，将会弹出询问框询问是否自动恢复，不再静默自动恢复任务
    任务设置=>运行选项=>时间段限制，现在可以不设置时间段，只匹配周几
    设置窗口总在最前，自动描边后，如果目标窗口被外部取消置顶，红边会自动取消
    首次安装时的示例任务做了一些改叫

    修复：
    屏幕变化检测时间单位识别为秒，导致触发延迟的问题
    屏幕变化检测无论如何都会首先触发一次的问题
    获取系统时间的各个子任务，界面无法展示的问题
    自动触发条件，屏幕颜色触发，毫秒显示为秒的问题

    其它改进和修复
ReleaseTime: 2025-12-23
