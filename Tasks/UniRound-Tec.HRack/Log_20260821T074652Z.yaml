Version: 0.3.6
Installer:
- Architecture: x64
  InstallerType: nullsoft
  InstallerUrl: https://github.com/UniRound-Tec/hrack/releases/download/v0.3.6/HRack-Setup-0.3.6.exe
Locale:
- Locale: zh-CN
  Key: ReleaseNotes
  Value: |-
    [0.3.6] - 2026-08-21
    新增
    - 支持重启 DSH 进程：打开 DSH 时标题栏提供「重启 DSH」，设置 → 会话里也有同一入口。安装插件后会杀掉当前 host 再拉起，并重载官方页面；HRack 的跟踪位不会被清掉。
    修复
    - 适配 DeepSeek Harness 0.1.0-rc.7+：dsh web 默认会打开系统浏览器，HRack 嵌入时传入 --no-open。WSL 上同版本 web 应用可能不认该参数，被拒绝后自动去掉再启动。
    - 适配 rc.7+ 官方页面模块加载：Cordis 改为从 window.__ModuleLoader__ 捕获，不再依赖已移除的 __DSH_MODULES__，避免嵌入失败后又弹出浏览器。
- Locale: zh-CN
  Key: ReleaseNotesUrl
  Value: https://github.com/UniRound-Tec/hrack/blob/HEAD/CHANGELOG.md
- Locale: zh-CN
  Key: ReleaseNotes
  Value: |-
    新增
    - 支持重启 DSH 进程：打开 DSH 时标题栏提供「重启 DSH」，设置 → 会话里也有同一入口。安装插件后会杀掉当前 host 再拉起，并重载官方页面；HRack 的跟踪位不会被清掉。
    修复
    - 适配 DeepSeek Harness 0.1.0-rc.7+：dsh web 默认会打开系统浏览器，HRack 嵌入时传入 --no-open。WSL 上同版本 web 应用可能不认该参数，被拒绝后自动去掉再启动。
    - 适配 rc.7+ 官方页面模块加载：Cordis 改为从 window.__ModuleLoader__ 捕获，不再依赖已移除的 __DSH_MODULES__，避免嵌入失败后又弹出浏览器。
- Locale: zh-CN
  Key: ReleaseNotesUrl
  Value: https://github.com/UniRound-Tec/hrack/blob/HEAD/CHANGELOG.md#036---2026-08-21
- Locale: zh-CN
  Key: ReleaseNotesUrl
  Value: https://github.com/UniRound-Tec/hrack/blob/HEAD/CHANGELOG.md
ReleaseTime: 2026-08-21T06:57:19.0000000Z
